language: node_js

#os: osx

node_js:
  - "node"
  - "8.11.4"
  - "10.9.0"

addons:
  firefox: "60.0.2"

git:
  depth: 3

before_deploy: scripts/travis-before_deploy.sh

deploy:
  provider: script
  skip_cleanup: true
  script: /tmp/deploy/travis-deploy.sh
  on:
    branch: master

cache:
  directories:
    - "node_modules"
